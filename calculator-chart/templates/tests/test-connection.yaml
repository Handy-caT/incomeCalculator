apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calculator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "calculator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calculator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
